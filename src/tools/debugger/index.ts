/*
import { VSCodeDebugger } from './vscode';
import { PythonDebugger } from './python';
import { DotNetDebugger } from './dotnet';
import { NodeDebugger } from './node';
import { DebuggerClient } from './types';

export const debuggerClients: Record<string, DebuggerClient> = {
  python: new PythonDebugger(),
  csharp: new DotNetDebugger(),
  nodejs: new NodeDebugger(),
  default: new VSCodeDebugger()
};
*/
